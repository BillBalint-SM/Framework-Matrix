{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:abk:schema:control-pilot:1.0.0",
  "title": "Bounded control-native pilot ledger",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "schema_version",
    "pilot_id",
    "campaign_id",
    "branch",
    "captured_at",
    "status",
    "full_campaign",
    "raw_runs",
    "expected_full_campaign_runs",
    "aligned_cases",
    "divergent_cases",
    "campaign_sha256",
    "schema_sha256",
    "control_manifest_sha256",
    "entrypoint_sha256",
    "pilot_schema_sha256",
    "results",
    "run_evidence_ids",
    "scorecard_state",
    "blockers"
  ],
  "properties": {
    "$schema": { "type": "string", "minLength": 1 },
    "schema_version": { "const": "1.0.0" },
    "pilot_id": { "type": "string", "pattern": "^abk:pilot:[a-z0-9-]+$" },
    "campaign_id": { "const": "abk:benchmark-campaign:artifact-dag-core-v1" },
    "branch": { "const": "control" },
    "captured_at": { "type": "string", "minLength": 20 },
    "status": { "const": "passed" },
    "full_campaign": { "const": false },
    "raw_runs": { "const": 10 },
    "expected_full_campaign_runs": { "const": 66 },
    "aligned_cases": { "type": "integer", "minimum": 0, "maximum": 10 },
    "divergent_cases": { "type": "integer", "minimum": 0, "maximum": 10 },
    "campaign_sha256": { "$ref": "#/definitions/sha256" },
    "schema_sha256": { "$ref": "#/definitions/sha256" },
    "control_manifest_sha256": { "$ref": "#/definitions/sha256" },
    "entrypoint_sha256": { "$ref": "#/definitions/sha256" },
    "pilot_schema_sha256": { "$ref": "#/definitions/sha256" },
    "results": {
      "type": "array",
      "minItems": 10,
      "maxItems": 10,
      "items": { "$ref": "#/definitions/result" }
    },
    "run_evidence_ids": {
      "type": "array",
      "minItems": 10,
      "maxItems": 10,
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[a-z0-9-]+$" }
    },
    "scorecard_state": { "const": "UNSCORED" },
    "blockers": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 20 } }
  },
  "definitions": {
    "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "result": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "evidence_id",
        "case_id",
        "status",
        "expected_terminal_state",
        "observed_terminal_state",
        "expected_exit_code",
        "observed_exit_code",
        "expected_error_code",
        "observed_error_code",
        "expected_recovery",
        "observed_recovery",
        "expected_handoff",
        "observed_handoff",
        "evaluation",
        "oracle_status",
        "run_relative_path",
        "run_sha256",
        "oracle_relative_path",
        "oracle_sha256"
      ],
      "properties": {
        "evidence_id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
        "case_id": { "type": "string", "pattern": "^(COM|SPC)-[0-9]{2}-[a-z][a-z0-9-]*$" },
        "status": { "const": "passed" },
        "expected_terminal_state": { "enum": ["SUCCEEDED", "RECOVERED", "STOPPED", "REJECTED", "FAILED"] },
        "observed_terminal_state": { "enum": ["SUCCEEDED", "RECOVERED", "STOPPED", "REJECTED", "FAILED"] },
        "expected_exit_code": { "type": "integer" },
        "observed_exit_code": { "type": "integer" },
        "expected_error_code": { "type": ["string", "null"] },
        "observed_error_code": { "type": ["string", "null"] },
        "expected_recovery": { "type": "boolean" },
        "observed_recovery": { "type": "boolean" },
        "expected_handoff": { "type": "boolean" },
        "observed_handoff": { "type": "boolean" },
        "evaluation": { "enum": ["aligned", "divergent"] },
        "oracle_status": { "enum": ["passed", "inconclusive"] },
        "run_relative_path": { "type": "string", "pattern": "^benchmarks/campaigns/artifact-dag-core-v1/runs/.+/run\\.json$" },
        "run_sha256": { "$ref": "#/definitions/sha256" },
        "oracle_relative_path": { "type": "string", "pattern": "^benchmarks/campaigns/artifact-dag-core-v1/runs/.+/oracle-result\\.json$" },
        "oracle_sha256": { "$ref": "#/definitions/sha256" }
      }
    }
  }
}
